# Generated at container start by caddy-entrypoint.sh from WEB_DOMAIN / ACME_EMAIL in .env.
# IP-only (WEB_DOMAIN empty): serve HTTP on :80 with /api/* → api, /* → web.
# Domain mode (WEB_DOMAIN set): auto-HTTPS on WEB_DOMAIN; bare-IP :80 redirects to https://WEB_DOMAIN.
